[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100

[tool.ruff.lint.per-file-ignores]
# Databricks notebooks use dbutils/spark/display globals and imports after widgets
"notebooks/**" = ["F821", "E402"]
